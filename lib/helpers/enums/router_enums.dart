enum RouterNames {
  home,
  history,
  analytic,
  plans,
}

enum HomeRouterNames {
  addCard,
  transactions,
  editAccount,
}

enum ChartSelected {
  liner,
  bar,
  pie,
}

enum CategoryDirection {
  left,
  right,
}
